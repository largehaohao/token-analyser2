# Coding Agent 成本分析

面向个人开发者，在会话运行中持续观察用量与可疑开销，并在任务结束后复盘，为后续工作提供改进依据。

## Language

**分析单元**：
一个主会话及有明确归属证据的子会话构成的统计范围，实时分析与任务复盘使用相同边界。一个分析单元可能覆盖多个用户需求；同一主会话恢复执行后仍属于原分析单元。
_Avoid_: 单个自然语言需求

**主会话**：
作为分析单元入口、直接承载用户请求的 Agent 会话。
_Avoid_: 主进程

**子会话**：
由明确的归属证据关联到其他会话、参与其执行的 Agent 会话。目录相同或时间接近本身不构成这种归属。
_Avoid_: 同目录会话

**执行主体**：
某笔已记录用量所归属的主会话或子会话。执行主体与行为归因是对同一笔用量的不同观察维度。
_Avoid_: 行为分类

**自身用量**：
归属于当前会话自身模型调用的用量，不包含其子会话的用量。
_Avoid_: 含子会话合计

**含子会话合计**：
覆盖当前会话及归属子会话的用量或费用合计。来源提供整体合计不意味着各子会话的明细也已具备。
_Avoid_: 自身用量与整体合计再次相加

**模型调用**：
一次可辨识的独立模型请求 / 响应循环，是逐轮用量明细的最小统计单位。一条日志、一次工具执行或一个用户请求都不自动等同于一次模型调用。
_Avoid_: 日志行、工具次数

**用户请求**：
用户提交的一项请求，可以引发多次模型调用及多个子会话的活动。
_Avoid_: 单次模型调用

**实时分析**：
针对运行中会话持续更新已观测用量、行为归因与疑似异常的分析。它不意味着尚未被来源记录的消耗已经可见。
_Avoid_: 实时扣费账本

**任务复盘**：
针对已经发生的编程任务，解释消耗去向、检查异常证据并形成后续改进建议的分析。
_Avoid_: 实时止损、自动干预

**已记录用量**：
来源会话资料中报告的消耗数量。它不自动等同于服务商实际扣款，也不证明相关行为存在浪费。
_Avoid_: 真实账单、已核实扣款

**行为归因**：
将已记录用量与对应的 Agent 行为关联起来的解释。它与来源直接报告的用量不同，可能包含推断。
_Avoid_: 官方计费分类

**混合行为**：
同一笔用量对应多种可识别行为，但现有证据不足以可靠拆分各行为用量的情况。
_Avoid_: 平均分摊后的精确费用

**未知归因**：
已有用量记录，但缺少足够证据判断对应行为的情况。
_Avoid_: 零消耗

**异常标签**：
对可疑行为类型的标注，描述同一笔用量的异常特征，不代表额外发生了一笔消耗。
_Avoid_: 新增计费项

**费用估算**：
依据已记录用量与适用费率计算的 credits 或货币金额，需与实际扣款区分。
_Avoid_: 已对账扣款

**费率快照**：
具有明确来源、核对日期和已知适用范围的一组计价依据。核对时仍有效的价格不自动适用于所有历史用量。
_Avoid_: 通用兑换系数

**数据不完整**：
现有记录缺少相关用量或估价依据，无法完整覆盖分析范围的状态。已知部分的小计不等同于完整总额。
_Avoid_: 无消耗、费用为零

**已知小计**：
仅覆盖当前可确认记录或可计价部分的合计，必须同时说明缺失范围。
_Avoid_: 完整总额

**关联调用费用**：
包含某项行为或异常的模型调用的估算费用。它不是该单个工具或行为的精确费用，也不证明这些费用都可以避免。
_Avoid_: 单次工具实际扣费

**纯异常调用**：
行为证据充分且可识别行为全部满足异常规则的模型调用。“纯”限定在可观察的行为范围，不证明该调用没有价值。
_Avoid_: 已证实无用的调用

**疑似可优化费用**：
按调用去重后，符合完整规则且可计价的纯异常调用的估算费用，排除首次正常基线操作及已标记必要的操作。它不是已经证实可以省下的金额。
_Avoid_: 已确认浪费、保证节省金额

**异常证据**：
支持某项可疑消耗判断的具体会话记录及其上下文。行为重复本身不构成行为没有必要的证明。
_Avoid_: 异常次数、重复次数

**异常置信度**：
现有证据支持某项异常判断的程度，以高、中、低表达。它不是未经校准的概率，也不代表对应费用一定能够被节省。
_Avoid_: 节省概率

**必要操作标记**：
用户认为某项被提示的行为对当前任务有必要的判断，只影响本次异常分析，不改变已记录用量。
_Avoid_: 删除消费记录

**日志静默**：
来源暂时没有产生可观察新记录的状态，不表示 Agent 已完成或其进程已经停止。
_Avoid_: 任务完成

**疑似重复读取**：
同一执行主体在局部时间窗口内反复读取同一文件的相同范围与内容的可疑现象。修改后的验证、新指令带来的读取和压缩后的首次恢复需要与其区分。
_Avoid_: 同路径访问次数

**疑似轮询空转**：
对同一目标反复查询、返回没有新输出或状态变化，却持续产生新增模型用量的可疑现象。
_Avoid_: 等待时长、所有 wait 调用

**疑似压缩循环**：
同一执行主体反复出现上下文压缩后重新读取相同内容的可疑序列。序列重复不自动证明任务没有进展。
_Avoid_: 所有压缩事件、没有代码改动

**原始会话日志**：
Coding Agent 生成的会话资料，可能包含对话、源代码、工具调用与输出、路径及用量记录。
_Avoid_: 纯计费数据
